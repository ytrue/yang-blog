<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yang.blog.mapper.AdminMapper">

    <resultMap type="java.util.HashMap" id="roleFindByIdAdminMap">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
    </resultMap>

    <select id="roleFindByIdAdmin" resultMap="roleFindByIdAdminMap">
        select
	        sys_role.id,
	        sys_role.code,
	        sys_role.name
		from
			sys_user_role
		left join
			sys_role
		on
			sys_user_role.role_id = sys_role.id
		where
			sys_user_role.user_id =#{id}
		order by
			id
    </select>

</mapper>
